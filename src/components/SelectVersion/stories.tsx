import { Story, Meta } from '@storybook/react/types-6-0'
import { SelectVersion } from '.'

export default {
  title: 'SelectVersion',
  component: SelectVersion,
} as Meta

export const Basic: Story = () => <SelectVersion />
